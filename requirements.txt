# Runtime dependencies — keep floors in sync with [project] dependencies in pyproject.toml.
# Pins illustrative at scaffold; resolve via pip freeze as needed.
httpx>=0.27
msal>=1.28
PyYAML>=6.0
pydantic>=2.0
pydantic-settings>=2.0
tenacity>=8.0
